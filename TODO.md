# TODOS
- Display chance in percent
- Import no need to add extension if it is .dice.
- Auto completion for imports / function names identifiers in interpreter.
- In the render display mean + variance somewhere.
- `total` should also accept unnamed axis
- Distributions should not include fields with probability 0.
- Have high precision rationals (need to be enabled)
 
