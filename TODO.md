# TODOS
- Cummulative distribution

## REPL
If a number rounded to an integer display it as an integer. 

## Unify standard library
The standard library now lives alongside the pure runtime functions in `diceengine.py` and is exposed to the interpreter through `executor.py`.
This should probably be unified to make usage from python easier.

## Re-Implement direct evaluation by having a "direct distrib" class. This class always collapses to 1 value immediately.

## Expectation over true / false
 ~(d20 + 6 >= [AC:10:15])
 should be probability that this is true.
 
 
